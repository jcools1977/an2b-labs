# TR-002r oracle seal record

Sealed 2026-09-03 at kickoff, after the reviewer's stamp and the three
amendments were frozen (protocol commit 11b914d), before Phase 0
closed and before any implementation code existed. Oracle:
claude-fable-5, fresh subagent, protocol text only, tool uses
verified 0 in the run metadata. The prediction plaintext is held out
of the repository (gitignored) until closeout, when it is committed
beside this hash and Brier-scored against the published verdict.

Disclosed limitation, same as TR-004: the prediction transited the
executor session's context at sealing time; isolation is
instruction-level, not structural. Mitigation: every gate is frozen
in the pre-registered protocol and reads through committed checker
code, so the bias surface is implementation choices only, each logged
in DECISIONS.md before its number.

The reviewer's context-rich forecast is the ledger's second column
and is sealed by the reviewer, not by this seat.

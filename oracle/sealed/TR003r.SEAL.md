# TR-003r oracle seal record (single seat, of record)

Sealed 2026-09-08 at kickoff, after the PI's stamp with three
conditions was applied and the protocol frozen (commit 05d156e, file
sha256 1d957068d911ee31f7305bd74c4fd1b350476ed84c380fedcef314dcacaa39a4),
before Phase 0 closed and before any implementation code existed.
Oracle: claude-fable-5-1, fresh subagent, protocol text pasted inline
and nothing else. Tool uses in the run metadata: exactly one, the
Write of the forecast JSON to the gitignored sealed path; the agent's
final report was the two words "written sealed". The plaintext did
NOT transit the builder's context: it was hashed and schema-checked
in place (validator output is a single line, no values). Plaintext
sha256: 045718d4f3e8ee00a100d6a5b6e0d3c16a5b87e0d68a36e75c9103b3c3717d40, committed in oracle/sealed/TR003r.sha256; the plaintext
stays gitignored until closeout, when it is committed beside this
hash and Brier-scored against the published verdict.

Isolation is instruction-level (no structural sandbox), as the
ledger discloses; this seal improves on TR-004 and TR-002r in one
respect: the forecast never entered the executor session. The
three-seat panel seals beside this one as the pilot's first subject
(oracle/panel/seals/TR-003r.*.json), reported beside and never
selected. The reviewer's context-rich forecast seals through their
own channel. A seal is a seal when this hash is pushed and fetched by
the reviewer's channel before closeout work begins.

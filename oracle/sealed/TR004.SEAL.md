# TR-004 oracle seal record

Sealed 2026-09-01 at kickoff, before Phase 0 closed and before any
implementation code existed. Oracle: claude-fable-5, fresh subagent,
protocol text only, tool uses verified 0. The prediction plaintext is
held out of the repository (gitignored) until closeout, when it is
committed beside this hash and Brier-scored against the published
verdict.

Disclosed limitation: the prediction transited the executor session's
context at sealing time. Mitigation: every gate is frozen in the
pre-registered protocol and reads through committed checker code, so
the bias surface is implementation choices only, each logged in
DECISIONS.md before its number.

# TR-004 utilization draft (PI ratifies; nothing here is decided)

**Proposed verdict: credibility-asset.**

The protocol's conditional deliverable (the curvature atlas feeding
TR-018) died with the verdict: the curvature leg read exactly
nothing, and TR-018 remains gated shut per the execution order.

What the FAIL yields:

1. **The noisy-exam result is independently citable methods
   equipment**: Two-NN, the field's default LID estimator, inverts
   dimensional ORDER at realistic noise and cloud size, caught by a
   pre-registered certification exam before it touched data. Every
   small-sample LID paper leans on estimators that never sat this
   exam. The exam itself (synthetic manifolds, noise condition,
   order preservation) is ~100 lines and travels anywhere.
2. **The certified estimator toolkit is reusable**: participation
   ratio and Levina-Bickel with their exam, the covariate
   residualizer with its intercept-trap bite-proof, the
   rogue-dimension control. The protocol promised a reusable
   LID/curvature toolkit; the LID half shipped certified.
3. **The finding itself is a careful negative with a live edge**: a
   real, small, cross-model intrinsic-dimension elevation around
   metaphor under the tightest identity/topic control in the
   program, honestly reported as under-gate. It is the kind of
   result that invites replication rather than headlines, which is
   the lab's brand.
4. **The credibility story**: five pre-registered verdicts in ten
   days, and the first one where the oracle ledger has a live
   before-the-fact forecast pair (protocol-only and context-rich) to
   score. The meta-experiment is now producing data.

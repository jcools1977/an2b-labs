# Import artifacts: original unzipped download, superseded by the
# protocol files at repo root (copied verbatim, diff-verified identical)
AN2BLabs/
*.zip

.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Large local artifacts never committed: model weights, cached latents
tr*/cache/
tr*/checkpoints/

# Raw downloads, reproducible via data/MANIFEST.json URLs and checksums
tr*/data/raw/
tr*/model_download.log
